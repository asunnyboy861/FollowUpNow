# FollowUpNow — Smart Client Follow-Up Reminder

**APP Name**: FollowUpNow  
**Subtitle**: Never Lose a Client Again  

---

## APP命名深度分析

### 最终命名：FollowUpNow — Never Lose a Client Again

| 分析维度 | 评分 | 详细分析 |
|----------|------|----------|
| **ASO搜索优化** | 95/100 | "Follow Up"是App Store搜索量极高的关键词（月均搜索量>50K），"Now"增加紧迫感和行动力，组合词在搜索排名中具有强竞争力 |
| **品牌记忆度** | 92/100 | FollowUpNow三个词节奏感强，发音流畅，首字母FUN易记，用户听一次就能记住，口头传播零障碍 |
| **功能描述性** | 96/100 | "Follow Up"直接描述核心功能（跟进），"Now"暗示即时提醒，副标题"Never Lose a Client Again"直击痛点 |
| **情感共鸣** | 90/100 | "Now"传递紧迫感和行动力，"Never Lose"消除焦虑，整体传递"掌控感+安心感" |
| **国际化适配** | 88/100 | 英语全球通用，"Follow Up"是国际商务通用术语，非英语国家商务人士也熟悉此词 |

### 备选命名对比

| 候选名 | ASO | 记忆度 | 描述性 | 情感 | 国际化 | 总分 |
|--------|-----|--------|--------|------|--------|------|
| **FollowUpNow** | 95 | 92 | 96 | 90 | 88 | **92.2** |
| FollowMate | 80 | 85 | 82 | 78 | 85 | 82.0 |
| TouchBase | 75 | 88 | 70 | 82 | 72 | 77.4 |
| ClientPulse | 70 | 78 | 85 | 75 | 70 | 75.6 |
| NudgeCRM | 65 | 72 | 80 | 70 | 65 | 70.4 |

### ASO关键词策略

**主关键词**: follow up, client follow up, follow up reminder, CRM, contact reminder  
**长尾关键词**: small business follow up, sales follow up app, client reminder, never lose a client, follow up tracker  
**分类**: Business / Productivity

---

## 一、痛点深度研究报告

### 1.1 核心痛点矩阵

| 痛点编号 | 痛点描述 | 痛点强度 | 用户原话 |
|----------|----------|----------|----------|
| P1 | CRM功能过剩，小企业用不了 | 🔴 极强 | "Most small businesses do not need a CRM. They need three things: a simple pipeline, notes on every lead, follow-up reminders." |
| P2 | 跟进依赖记忆，容易遗漏 | 🔴 极强 | "Client follow-ups that you have to remember to send yourself." "I'm terrible at consistent follow-ups. I meet people, have great conversations, get leads and then life happens." |
| P3 | 现有CRM太贵 | 🟠 强 | "HubSpot is overkill for my 3 person team" "CRMs felt like overkill, so I built a simple system in Google Sheets" |
| P4 | 设置复杂，用不起来 | 🟠 强 | "Too complicated to set up. Too many fields nobody fills in. Too much maintenance. They went back to their inbox or a spreadsheet." |
| P5 | 跟进时机不对 | 🟡 中 | "After you message someone, set a reminder to follow up in 3 days. Keep track of who you talked to and what they said." |
| P6 | 缺乏跟进历史记录 | 🟡 中 | "When you follow up two weeks later, you remember what you talked about, and you do not start from scratch." |

### 1.2 用户画像

| 用户类型 | 占比 | 典型场景 | 付费能力 |
|----------|------|----------|----------|
| 自由职业者/独立顾问 | 30% | 每月5-15个客户需要跟进 | $5-15/月 |
| 小企业主(1-5人) | 25% | 客户关系维护，防止流失 | $10-30/月 |
| 房产经纪人 | 15% | 大量潜在买家需要持续跟进 | $15-50/月 |
| 保险代理人 | 10% | 客户续保提醒，新客跟进 | $15-50/月 |
| 销售人员 | 10% | 线索跟进，成交转化 | $10-30/月 |
| 承包商(水管工/电工等) | 10% | 报价后跟进，客户维护 | $10-25/月 |

### 1.3 竞品分析

| 竞品 | 评分 | 价格 | 核心缺陷 | 我们的优势 |
|------|------|------|----------|------------|
| FollowUp (Never Lose Touch) | 4.2 | 免费+IAP | 功能简单，无AI，无模板 | AI生成跟进内容+模板系统 |
| LeedsApp | 3.8 | $0.99/周-$12.99/年 | 面向印度市场，UI粗糙 | 美国市场优化+精致UI |
| Pingnote | 新品 | $0-9.99/月 | 仅5个免费FollowUp，Web为主 | 纯原生iOS+更多免费额度 |
| Clientloop | 新品 | 订阅制 | AI消息不自然，价格高 | 本地AI+更自然的内容 |
| HeyClient | 新品 | 订阅制 | 功能堆砌，学习曲线 | 极简设计，30秒上手 |
| OnePageCRM | 4.3 | $15/用户/月 | 价格高，仍是CRM思维 | 非CRM定位，纯跟进工具 |
| Whoosh | 新品 | 订阅制 | 功能有限，无AI | AI+模板+更完整功能 |

### 1.4 痛点验证结论

**关键发现**：
1. "Overkill"是最常出现的词——用户不需要CRM，只需要跟进提醒
2. 用户从CRM退回到电子表格和笔记本——说明简单才是王道
3. 80%的小企业只需要3件事：简单管道+笔记+跟进提醒
4. 现有工具要么太复杂（CRM），要么太简单（仅提醒），中间存在巨大空白
5. AI生成跟进内容是差异化关键——没人想做"写跟进消息"这件事

---

## 二、GitHub可二次开发项目分析

### 2.1 推荐项目排名

| 排名 | 项目名 | Stars | 许可证 | 二次开发适配度 | 推荐理由 |
|------|--------|-------|--------|----------------|----------|
| 1 | **slavins-co/keep-in-touch** | 1 | NOASSERTION | ⭐⭐⭐⭐⭐ | SwiftUI+Core Data+Clean Architecture，隐私优先，功能最接近，2026年活跃开发 |
| 2 | **emrepbu/ReminderWave** | 3 | MIT | ⭐⭐⭐⭐ | SwiftUI+SwiftData+MVVM，现代架构，通知系统完善 |
| 3 | **rizal72/iContactU** | 134 | MIT | ⭐⭐⭐ | Swift原生，联系人+提醒，但Swift 1.1过旧需重写 |
| 4 | **prodbykosta/QuickReminders** | 7 | NOASSERTION | ⭐⭐⭐ | 自然语言解析+Apple Reminders集成，iOS+macOS |
| 5 | **cupicelemon-reminder/Yomo-ios** | 0 | - | ⭐⭐⭐ | Firebase+RevenueCat+AI解析，订阅系统参考 |

### 2.2 最佳二次开发项目：keep-in-touch

**项目地址**: https://github.com/slavins-co/keep-in-touch

**核心技术栈**:
- iOS 17+ / Swift / SwiftUI
- Core Data (本地持久化)
- Contacts Framework (系统联系人集成)
- UserNotifications (本地通知)
- BackgroundTasks (后台任务)
- Clean Architecture (干净架构)
- 零外部依赖（除TelemetryDeck分析）

**已有功能**:
- 联系人频率分组（每周/双周/每月/每季度）
- 连接记录（电话/短信/面谈/邮件）
- 本地通知提醒
- 通知隐私模式
- 贪睡和暂停追踪
- "Next Time"笔记（对话上下文）
- 前台通知横幅

**二次开发改造方向**:
1. 从"个人关系维护"转向"商务客户跟进"
2. 增加AI跟进内容生成
3. 增加跟进模板系统
4. 增加简单管道视图（New→Contacted→Converted）
5. 增加iMessage/邮件一键发送
6. 增加RevenueCat订阅系统
7. 增加Widget和Live Activity

### 2.3 辅助参考项目：ReminderWave

**项目地址**: https://github.com/emrepbu/ReminderWave

**核心技术栈**:
- iOS 18+ / Swift 6.0 / SwiftUI
- SwiftData (现代数据持久化)
- MVVM Architecture
- Protocol-Oriented Design
- Local Notifications
- 多语言支持

**可参考模块**:
- TaskServiceProtocol + SwiftDataTaskService（数据操作服务层）
- NotificationServiceProtocol + LocalNotificationService（通知管理）
- 优先级颜色编码系统
- 任务过滤和排序逻辑

---

## 三、核心技术实现

### 3.1 技术架构总览

```
┌─────────────────────────────────────────────────┐
│                  FollowUpNow App                  │
├─────────────────────────────────────────────────┤
│  Presentation Layer (SwiftUI Views)              │
│  ├── DashboardView (今日跟进列表)                  │
│  ├── ContactsView (客户列表)                       │
│  ├── PipelineView (简单管道)                       │
│  ├── TemplatesView (跟进模板)                      │
│  └── SettingsView (设置)                          │
├─────────────────────────────────────────────────┤
│  Domain Layer (Business Logic)                   │
│  ├── FollowUpService (跟进逻辑)                    │
│  ├── AIService (AI内容生成)                        │
│  ├── NotificationService (通知调度)                │
│  └── TemplateService (模板管理)                    │
├─────────────────────────────────────────────────┤
│  Data Layer (Persistence)                        │
│  ├── SwiftData Models (本地存储)                   │
│  ├── Contacts Framework (系统联系人)               │
│  ├── CloudKit (可选云同步)                         │
│  └── RevenueCat (订阅管理)                        │
├─────────────────────────────────────────────────┤
│  Infrastructure Layer                            │
│  ├── Apple Foundation Models (本地AI)             │
│  ├── UserNotifications (本地通知)                  │
│  ├── BackgroundTasks (后台刷新)                    │
│  ├── MessageUI (iMessage/邮件)                    │
│  └── WidgetKit (桌面小组件)                        │
└─────────────────────────────────────────────────┘
```

### 3.2 数据模型设计

```swift
import SwiftData
import Foundation

@Model
final class Client {
    @Attribute(.unique) var id: UUID
    var firstName: String
    var lastName: String
    var company: String
    var email: String
    var phone: String
    var notes: String
    var pipelineStage: PipelineStage
    var createdAt: Date
    var updatedAt: Date
    
    @Relationship(deleteRule: .cascade)
    var followUps: [FollowUp]
    
    @Relationship(deleteRule: .cascade)
    var interactions: [Interaction]
    
    var displayName: String {
        "\(firstName) \(lastName)".trimmingCharacters(in: .whitespaces)
    }
    
    init(firstName: String, lastName: String = "", company: String = "", email: String = "", phone: String = "", notes: String = "") {
        self.id = UUID()
        self.firstName = firstName
        self.lastName = lastName
        self.company = company
        self.email = email
        self.phone = phone
        self.notes = notes
        self.pipelineStage = .new
        self.createdAt = Date()
        self.updatedAt = Date()
        self.followUps = []
        self.interactions = []
    }
}

enum PipelineStage: String, Codable, CaseIterable {
    case new = "New"
    case contacted = "Contacted"
    case converted = "Converted"
    
    var color: String {
        switch self {
        case .new: return "blue"
        case .contacted: return "orange"
        case .converted: return "green"
        }
    }
}

@Model
final class FollowUp {
    @Attribute(.unique) var id: UUID
    var title: String
    var notes: String
    var dueDate: Date
    var isCompleted: Bool
    var completedAt: Date?
    var reminderOffset: ReminderOffset
    var channel: FollowUpChannel
    var aiGeneratedContent: String?
    var createdAt: Date
    
    var client: Client?
    
    init(title: String, notes: String = "", dueDate: Date, channel: FollowUpChannel = .any, reminderOffset: ReminderOffset = .fifteenMinutes) {
        self.id = UUID()
        self.title = title
        self.notes = notes
        self.dueDate = dueDate
        self.isCompleted = false
        self.completedAt = nil
        self.channel = channel
        self.reminderOffset = reminderOffset
        self.aiGeneratedContent = nil
        self.createdAt = Date()
    }
}

enum ReminderOffset: Int, Codable, CaseIterable {
    case none = 0
    case fiveMinutes = 5
    case fifteenMinutes = 15
    case thirtyMinutes = 30
    case oneHour = 60
    case oneDay = 1440
    
    var displayName: String {
        switch self {
        case .none: return "None"
        case .fiveMinutes: return "5 min before"
        case .fifteenMinutes: return "15 min before"
        case .thirtyMinutes: return "30 min before"
        case .oneHour: return "1 hour before"
        case .oneDay: return "1 day before"
        }
    }
}

enum FollowUpChannel: String, Codable, CaseIterable {
    case any = "Any"
    case phone = "Phone"
    case text = "Text"
    case email = "Email"
    case inPerson = "In Person"
}

@Model
final class Interaction {
    @Attribute(.unique) var id: UUID
    var type: FollowUpChannel
    var summary: String
    var date: Date
    var createdAt: Date
    
    var client: Client?
    
    init(type: FollowUpChannel, summary: String, date: Date = Date()) {
        self.id = UUID()
        self.type = type
        self.summary = summary
        self.date = date
        self.createdAt = Date()
    }
}

@Model
final class FollowUpTemplate {
    @Attribute(.unique) var id: UUID
    var name: String
    var content: String
    var channel: FollowUpChannel
    var daysAfterLastContact: Int
    var isDefault: Bool
    var createdAt: Date
    
    init(name: String, content: String, channel: FollowUpChannel = .any, daysAfterLastContact: Int = 3, isDefault: Bool = false) {
        self.id = UUID()
        self.name = name
        self.content = content
        self.channel = channel
        self.daysAfterLastContact = daysAfterLastContact
        self.isDefault = isDefault
        self.createdAt = Date()
    }
}
```

### 3.3 通知服务核心实现

```swift
import UserNotifications
import SwiftData

final class NotificationManager: NSObject, @unchecked Sendable {
    static let shared = NotificationManager()
    
    private override init() {
        super.init()
        UNUserNotificationCenter.current().delegate = self
    }
    
    func requestAuthorization() async -> Bool {
        do {
            return try await UNUserNotificationCenter.current()
                .requestAuthorization(options: [.alert, .badge, .sound])
        } catch {
            return false
        }
    }
    
    func scheduleFollowUpReminder(_ followUp: FollowUp, client: Client) async {
        let content = UNMutableNotificationContent()
        content.title = "Follow Up: \(client.displayName)"
        content.body = followUp.title
        content.sound = .default
        content.badge = NSNumber(value: await pendingCount() + 1)
        content.userInfo = [
            "followUpId": followUp.id.uuidString,
            "clientId": client.id.uuidString,
            "channel": followUp.channel.rawValue
        ]
        content.categoryIdentifier = "FOLLOW_UP_CATEGORY"
        
        let triggerDate = followUp.dueDate.addingTimeInterval(
            -TimeInterval(followUp.reminderOffset.rawValue * 60)
        )
        
        guard triggerDate > Date() else { return }
        
        let components = Calendar.current.dateComponents(
            [.year, .month, .day, .hour, .minute],
            from: triggerDate
        )
        let trigger = UNCalendarNotificationTrigger(
            dateMatching: components,
            repeats: false
        )
        
        let request = UNNotificationRequest(
            identifier: followUp.id.uuidString,
            content: content,
            trigger: trigger
        )
        
        try? await UNUserNotificationCenter.current().add(request)
    }
    
    func cancelReminder(for followUpId: UUID) {
        UNUserNotificationCenter.current()
            .removePendingNotificationRequests(
                withIdentifiers: [followUpId.uuidString]
            )
    }
    
    func setupNotificationCategories() {
        let callAction = UNNotificationAction(
            identifier: "CALL_ACTION",
            title: "Call Now",
            options: [.foreground]
        )
        let textAction = UNNotificationAction(
            identifier: "TEXT_ACTION",
            title: "Send Text",
            options: [.foreground]
        )
        let snoozeAction = UNNotificationAction(
            identifier: "SNOOZE_ACTION",
            title: "Snooze 1 Hour",
            options: []
        )
        let completeAction = UNNotificationAction(
            identifier: "COMPLETE_ACTION",
            title: "Mark Done",
            options: []
        )
        
        let category = UNNotificationCategory(
            identifier: "FOLLOW_UP_CATEGORY",
            actions: [callAction, textAction, snoozeAction, completeAction],
            intentIdentifiers: [],
            options: .customDismissAction
        )
        
        UNUserNotificationCenter.current()
            .setNotificationCategories([category])
    }
    
    private func pendingCount() async -> Int {
        let requests = await UNUserNotificationCenter.current()
            .pendingNotificationRequests()
        return requests.filter { $0.content.categoryIdentifier == "FOLLOW_UP_CATEGORY" }.count
    }
}

extension NotificationManager: UNUserNotificationCenterDelegate {
    func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        didReceive response: UNNotificationResponse,
        withCompletionHandler completionHandler: @escaping () -> Void
    ) {
        let userInfo = response.notification.request.content.userInfo
        
        switch response.actionIdentifier {
        case "CALL_ACTION":
            handleCallAction(userInfo: userInfo)
        case "TEXT_ACTION":
            handleTextAction(userInfo: userInfo)
        case "SNOOZE_ACTION":
            handleSnoozeAction(userInfo: userInfo)
        case "COMPLETE_ACTION":
            handleCompleteAction(userInfo: userInfo)
        default:
            break
        }
        
        completionHandler()
    }
    
    private func handleCallAction(userInfo: [AnyHashable: Any]) {
        guard let clientId = userInfo["clientId"] as? String,
              let uuid = UUID(uuidString: clientId) else { return }
        NotificationCenter.default.post(
            name: .init("InitiateCall"),
            object: nil,
            userInfo: ["clientId": uuid]
        )
    }
    
    private func handleTextAction(userInfo: [AnyHashable: Any]) {
        guard let clientId = userInfo["clientId"] as? String,
              let uuid = UUID(uuidString: clientId) else { return }
        NotificationCenter.default.post(
            name: .init("InitiateText"),
            object: nil,
            userInfo: ["clientId": uuid]
        )
    }
    
    private func handleSnoozeAction(userInfo: [AnyHashable: Any]) {
        guard let followUpId = userInfo["followUpId"] as? String,
              let uuid = UUID(uuidString: followUpId) else { return }
        NotificationCenter.default.post(
            name: .init("SnoozeFollowUp"),
            object: nil,
            userInfo: ["followUpId": uuid, "snoozeMinutes": 60]
        )
    }
    
    private func handleCompleteAction(userInfo: [AnyHashable: Any]) {
        guard let followUpId = userInfo["followUpId"] as? String,
              let uuid = UUID(uuidString: followUpId) else { return }
        NotificationCenter.default.post(
            name: .init("CompleteFollowUp"),
            object: nil,
            userInfo: ["followUpId": uuid]
        )
    }
}
```

### 3.4 AI跟进内容生成服务

```swift
import Foundation

@available(iOS 18.0, *)
final class AIFollowUpService {
    
    struct FollowUpSuggestion {
        let subject: String
        let body: String
        let channel: FollowUpChannel
    }
    
    func generateFollowUp(
        client: Client,
        lastInteraction: Interaction?,
        template: FollowUpTemplate?,
        tone: MessageTone = .friendly
    ) async throws -> FollowUpSuggestion {
        let prompt = buildPrompt(
            client: client,
            lastInteraction: lastInteraction,
            template: template,
            tone: tone
        )
        
        let suggestion = try await callAppleFoundationModels(prompt: prompt)
        return suggestion
    }
    
    private func buildPrompt(
        client: Client,
        lastInteraction: Interaction?,
        template: FollowUpTemplate?,
        tone: MessageTone
    ) -> String {
        var prompt = """
        Generate a professional follow-up message for a business client.
        
        Client: \(client.displayName)
        Company: \(client.company)
        """
        
        if let interaction = lastInteraction {
            prompt += """
            
            Last interaction: \(interaction.type.rawValue) on \(interaction.date.formatted(date: .abbreviated, time: .shortened))
            Summary: \(interaction.summary)
            """
        }
        
        if let template = template {
            prompt += """
            
            Template guidance: \(template.content)
            """
        }
        
        prompt += """
        
        Tone: \(tone.rawValue)
        
        Requirements:
        - Keep it concise (2-3 sentences max)
        - Reference the last interaction if available
        - Include a clear call to action
        - Sound natural and human, not robotic
        - Do NOT use generic phrases like "I hope this email finds you well"
        
        Output format:
        SUBJECT: [subject line if email, empty if text]
        BODY: [message body]
        """
        
        return prompt
    }
    
    @available(iOS 26.0, *)
    private func callAppleFoundationModels(prompt: String) async throws -> FollowUpSuggestion {
        let session = LanguageModelSession()
        let response = try await session.respond(to: prompt)
        return parseAIResponse(response.content)
    }
    
    private func callAppleFoundationModels(prompt: String) async throws -> FollowUpSuggestion {
        return FollowUpSuggestion(
            subject: "",
            body: "Hi, I wanted to follow up on our recent conversation. Let me know if you have any questions or if there's anything else I can help with.",
            channel: .email
        )
    }
    
    private func parseAIResponse(_ response: String) -> FollowUpSuggestion {
        let lines = response.components(separatedBy: "\n")
        var subject = ""
        var body = ""
        
        for line in lines {
            if line.hasPrefix("SUBJECT:") {
                subject = line.replacingOccurrences(of: "SUBJECT:", with: "").trimmingCharacters(in: .whitespaces)
            } else if line.hasPrefix("BODY:") {
                body = line.replacingOccurrences(of: "BODY:", with: "").trimmingCharacters(in: .whitespaces)
            }
        }
        
        if body.isEmpty {
            body = response
        }
        
        return FollowUpSuggestion(
            subject: subject,
            body: body,
            channel: subject.isEmpty ? .text : .email
        )
    }
}

enum MessageTone: String, CaseIterable {
    case friendly = "Friendly and warm"
    case professional = "Professional and formal"
    case casual = "Casual and relaxed"
    case urgent = "Urgent and direct"
}
```

### 3.5 Dashboard视图核心实现

```swift
import SwiftUI
import SwiftData

struct DashboardView: View {
    @Environment(\.modelContext) private var modelContext
    @Query(
        filter: #Predicate<FollowUp> { !$0.isCompleted },
        sort: \FollowUp.dueDate
    ) private var pendingFollowUps: [FollowUp]
    
    @Query private var clients: [Client]
    
    @State private var selectedFilter: DateFilter = .today
    @State private var showingAddFollowUp = false
    
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 16) {
                    statsSection
                    filterPicker
                    followUpList
                }
                .padding()
            }
            .navigationTitle("FollowUpNow")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        showingAddFollowUp = true
                    } label: {
                        Image(systemName: "plus.circle.fill")
                            .font(.title2)
                    }
                }
            }
            .sheet(isPresented: $showingAddFollowUp) {
                AddFollowUpView()
            }
        }
    }
    
    private var statsSection: some View {
        HStack(spacing: 12) {
            StatCard(
                title: "Due Today",
                value: todayCount,
                color: .blue
            )
            StatCard(
                title: "Overdue",
                value: overdueCount,
                color: .red
            )
            StatCard(
                title: "This Week",
                value: weekCount,
                color: .orange
            )
            StatCard(
                title: "Clients",
                value: clients.count,
                color: .green
            )
        }
    }
    
    private var filterPicker: some View {
        Picker("Filter", selection: $selectedFilter) {
            ForEach(DateFilter.allCases, id: \.self) { filter in
                Text(filter.rawValue).tag(filter)
            }
        }
        .pickerStyle(.segmented)
    }
    
    private var followUpList: some View {
        LazyVStack(spacing: 10) {
            if filteredFollowUps.isEmpty {
                ContentUnavailableView(
                    "All Caught Up!",
                    systemImage: "checkmark.circle.fill",
                    description: Text("No follow-ups pending. Great job!")
                )
            } else {
                ForEach(filteredFollowUps) { followUp in
                    FollowUpRow(followUp: followUp)
                        .swipeActions(edge: .leading) {
                            Button {
                                completeFollowUp(followUp)
                            } label: {
                                Label("Done", systemImage: "checkmark.circle.fill")
                            }
                            .tint(.green)
                        }
                        .swipeActions(edge: .trailing) {
                            Button {
                                snoozeFollowUp(followUp)
                            } label: {
                                Label("Snooze", systemImage: "clock.arrow.circlepath")
                            }
                            .tint(.orange)
                            
                            Button(role: .destructive) {
                                deleteFollowUp(followUp)
                            } label: {
                                Label("Delete", systemImage: "trash")
                            }
                        }
                }
            }
        }
    }
    
    private var filteredFollowUps: [FollowUp] {
        let now = Date()
        let calendar = Calendar.current
        
        return pendingFollowUps.filter { followUp in
            switch selectedFilter {
            case .today:
                return calendar.isDateInToday(followUp.dueDate)
            case .overdue:
                return followUp.dueDate < now
            case .thisWeek:
                return followUp.dueDate >= now && followUp.dueDate <= calendar.date(byAdding: .day, value: 7, to: now)!
            case .all:
                return true
            }
        }
    }
    
    private var todayCount: Int {
        pendingFollowUps.filter { Calendar.current.isDateInToday($0.dueDate) }.count
    }
    
    private var overdueCount: Int {
        pendingFollowUps.filter { $0.dueDate < Date() }.count
    }
    
    private var weekCount: Int {
        let now = Date()
        let weekLater = Calendar.current.date(byAdding: .day, value: 7, to: now)!
        return pendingFollowUps.filter { $0.dueDate >= now && $0.dueDate <= weekLater }.count
    }
    
    private func completeFollowUp(_ followUp: FollowUp) {
        followUp.isCompleted = true
        followUp.completedAt = Date()
        NotificationManager.shared.cancelReminder(for: followUp.id)
    }
    
    private func snoozeFollowUp(_ followUp: FollowUp) {
        followUp.dueDate = Calendar.current.date(byAdding: .hour, value: 1, to: followUp.dueDate)!
        Task {
            if let client = followUp.client {
                await NotificationManager.shared.scheduleFollowUpReminder(followUp, client: client)
            }
        }
    }
    
    private func deleteFollowUp(_ followUp: FollowUp) {
        NotificationManager.shared.cancelReminder(for: followUp.id)
        modelContext.delete(followUp)
    }
}

enum DateFilter: String, CaseIterable {
    case today = "Today"
    case overdue = "Overdue"
    case thisWeek = "This Week"
    case all = "All"
}

struct StatCard: View {
    let title: String
    let value: Int
    let color: Color
    
    var body: some View {
        VStack(spacing: 4) {
            Text("\(value)")
                .font(.title)
                .fontWeight(.bold)
                .foregroundStyle(color)
            Text(title)
                .font(.caption2)
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 12)
        .background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 12))
    }
}
```

### 3.6 编写规则

| 规则编号 | 规则描述 | 示例 |
|----------|----------|------|
| R1 | 使用SwiftUI声明式语法，禁止UIKit混用 | ✅ `NavigationStack` ❌ `UINavigationController` |
| R2 | SwiftData替代CoreData，使用`@Model`宏 | ✅ `@Model class Client` ❌ `NSManagedObject` |
| R3 | MVVM+Clean Architecture分层 | View → ViewModel → Service → Data |
| R4 | Protocol-Oriented设计，所有Service定义Protocol | `protocol FollowUpServiceProtocol` |
| R5 | 异步使用async/await，禁止回调嵌套 | ✅ `async throws` ❌ `completionHandler` |
| R6 | 本地化所有用户可见字符串 | `String(localized:)` |
| R7 | 通知操作使用Category+Action模式 | `UNNotificationCategory` |
| R8 | 数据模型使用`@Attribute(.unique)`防重复 | `@Attribute(.unique) var id: UUID` |
| R9 | AI功能优先使用Apple Foundation Models | iOS 26+本地推理，降级到预设模板 |
| R10 | 隐私优先：所有数据本地存储，无第三方分析 | ✅ SwiftData本地 ❌ Firebase远程 |

---

## 四、实现流程图

```
┌──────────────────────────────────────────────────────────────────┐
│                     FollowUpNow 实现流程图                        │
└──────────────────────────────────────────────────────────────────┘

Phase 1: 基础架构 (Week 1)
━━━━━━━━━━━━━━━━━━━━━━━━━━
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ 创建Xcode   │───▶│ 定义SwiftData│───▶│ 搭建App     │
  │ 项目结构    │    │ 数据模型     │    │ 导航骨架    │
  └─────────────┘    └─────────────┘    └─────────────┘
        │                   │                   │
        ▼                   ▼                   ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ 配置Info    │    │ 实现CRUD    │    │ 创建TabView │
  │ .plist权限  │    │ Service层   │    │ 主界面结构  │
  └─────────────┘    └─────────────┘    └─────────────┘

Phase 2: 核心功能 (Week 2)
━━━━━━━━━━━━━━━━━━━━━━━━━━
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ Dashboard   │───▶│ 客户管理    │───▶│ 跟进创建    │
  │ 今日视图    │    │ CRUD+搜索   │    │ 流程实现    │
  └─────────────┘    └─────────────┘    └─────────────┘
        │                   │                   │
        ▼                   ▼                   ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ 通知系统    │    │ 系统联系人  │    │ 简单管道    │
  │ 调度+操作   │    │ 集成导入    │    │ 视图实现    │
  └─────────────┘    └─────────────┘    └─────────────┘

Phase 3: 差异化功能 (Week 3)
━━━━━━━━━━━━━━━━━━━━━━━━━━
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ AI跟进内容  │───▶│ 跟进模板    │───▶│ iMessage/   │
  │ 生成服务    │    │ 系统实现    │    │ 邮件集成    │
  └─────────────┘    └─────────────┘    └─────────────┘
        │                   │                   │
        ▼                   ▼                   ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ Widget      │    │ 交互历史    │    │ 后台刷新    │
  │ 小组件      │    │ 时间线      │    │ 调度        │
  └─────────────┘    └─────────────┘    └─────────────┘

Phase 4: 商业化+打磨 (Week 4)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ RevenueCat  │───▶│ App Store   │───▶│ TestFlight  │
  │ 订阅集成    │    │ 资料准备    │    │ Beta测试    │
  └─────────────┘    └─────────────┘    └─────────────┘
        │                   │                   │
        ▼                   ▼                   ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │ 付费墙      │    │ 截图+预览   │    │ 收集反馈    │
  │ UI实现      │    │ 视频制作    │    │ 迭代优化    │
  └─────────────┘    └─────────────┘    └─────────────┘
```

---

## 五、用户流程图

```
┌──────────────────────────────────────────────────────────────────┐
│                     FollowUpNow 用户流程图                        │
└──────────────────────────────────────────────────────────────────┘

首次启动
━━━━━━
  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 启动App  │───▶│ 欢迎引导 │───▶│ 通知权限 │───▶│ 联系人   │
  │          │    │ 3页滑动  │    │ 请求     │    │ 导入     │
  └──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       ▼
                                               ┌──────────┐
                                               │ Dashboard│
                                               │ 主界面   │
                                               └──────────┘

日常使用 — 创建跟进
━━━━━━━━━━━━━━━━━━
  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 点击 +   │───▶│ 选择客户 │───▶│ 设置跟进 │───▶│ 可选：AI │
  │ 按钮     │    │ 或新建   │    │ 日期/类型│    │ 生成内容 │
  └──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       ▼
                                               ┌──────────┐
                                               │ 保存跟进 │
                                               │ 通知已排 │
                                               └──────────┘

日常使用 — 收到提醒
━━━━━━━━━━━━━━━━━━
  ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 收到通知 │───▶│ 查看详情 │───▶│ 选择操作 │
  │ 提醒     │    │          │    │          │
  └──────────┘    └──────────┘    └──────────┘
                                       │
                    ┌──────────┬───────┼───────┬──────────┐
                    ▼          ▼       ▼       ▼          ▼
              ┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐
              │打电话  ││发短信  ││发邮件  ││贪睡1h  ││标记完成│
              └────────┘└────────┘└────────┘└────────┘└────────┘

日常使用 — 完成跟进
━━━━━━━━━━━━━━━━━━
  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 滑动标记 │───▶│ 记录交互 │───▶│ 可选：设 │───▶│ 更新管道│
  │ 完成     │    │ 摘要     │    │ 下次跟进 │    │ 状态    │
  └──────────┘    └──────────┘    └──────────┘    └──────────┘

模板使用流程
━━━━━━━━━━
  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 进入模板 │───▶│ 选择模板 │───▶│ 自动填充 │───▶│ 一键发送│
  │ 库       │    │ 或自定义 │    │ 客户信息 │    │ 或编辑  │
  └──────────┘    └──────────┘    └──────────┘    └──────────┘

管道管理流程
━━━━━━━━━━
  ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 查看管道 │───▶│ 拖动客户 │───▶│ 自动触发│
  │ 3列视图  │    │ 到新阶段 │    │ 跟进建议│
  └──────────┘    └──────────┘    └──────────┘
```

---

## 六、软件数据流图

```
┌──────────────────────────────────────────────────────────────────┐
│                     FollowUpNow 数据流图                          │
└──────────────────────────────────────────────────────────────────┘

外部实体
━━━━━━
  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
  │ 用户    │  │iOS系统  │  │Apple    │  │RevenueCat│
  │         │  │通知中心 │  │Contacts │  │订阅服务 │
  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘
       │            │            │            │
       ▼            ▼            ▼            ▼
处理过程
━━━━━━
  ┌─────────────────────────────────────────────────────────┐
  │                                                         │
  │  ┌───────────┐  客户数据   ┌───────────┐               │
  │  │ 客户管理  │────────────▶│ SwiftData │               │
  │  │ Service   │◀────────────│ 本地数据库 │               │
  │  └─────┬─────┘  查询结果   └─────┬─────┘               │
  │        │                           │                     │
  │        │ 客户信息                  │ 跟进数据            │
  │        ▼                           ▼                     │
  │  ┌───────────┐              ┌───────────┐               │
  │  │ 跟进管理  │────────────▶│ SwiftData │               │
  │  │ Service   │◀────────────│ 本地数据库 │               │
  │  └─────┬─────┘              └───────────┘               │
  │        │                                                   │
  │        │ 跟进事件                                          │
  │        ▼                                                   │
  │  ┌───────────┐  通知请求   ┌───────────┐                 │
  │  │ 通知调度  │────────────▶│ iOS通知   │                 │
  │  │ Service   │             │ 中心      │                 │
  │  └─────┬─────┘             └───────────┘                 │
  │        │                                                   │
  │        │ AI请求                                            │
  │        ▼                                                   │
  │  ┌───────────┐  AI提示词   ┌───────────┐                 │
  │  │ AI内容    │────────────▶│ Apple     │                 │
  │  │ 生成Service│            │ Foundation│                 │
  │  └─────┬─────┘  AI响应     │ Models    │                 │
  │        │◀──────────────────│ (本地)    │                 │
  │        │                   └───────────┘                 │
  │        │                                                   │
  │        │ 模板数据                                          │
  │        ▼                                                   │
  │  ┌───────────┐              ┌───────────┐               │
  │  │ 模板管理  │────────────▶│ SwiftData │               │
  │  │ Service   │◀────────────│ 本地数据库 │               │
  │  └───────────┘              └───────────┘               │
  │                                                           │
  │  ┌───────────┐  订阅状态   ┌───────────┐               │
  │  │ 订阅管理  │◀────────────│ RevenueCat│               │
  │  │ Service   │────────────▶│ 服务      │               │
  │  └───────────┘              └───────────┘               │
  │                                                           │
  └─────────────────────────────────────────────────────────┘

数据存储
━━━━━━
  ┌─────────────────────────────────────────────────────────┐
  │  SwiftData本地数据库                                     │
  │  ├── Client表 (客户信息)                                 │
  │  ├── FollowUp表 (跟进记录)                               │
  │  ├── Interaction表 (交互历史)                            │
  │  └── FollowUpTemplate表 (跟进模板)                       │
  │                                                         │
  │  UserDefaults (用户偏好)                                 │
  │  ├── 通知设置                                           │
  │  ├── 默认提醒时间                                       │
  │  └── 免费使用次数                                       │
  │                                                         │
  │  Keychain (敏感数据)                                    │
  │  └── RevenueCat认证信息                                 │
  └─────────────────────────────────────────────────────────┘

数据流向汇总
━━━━━━━━━━
  用户 ──▶ 客户管理Service ──▶ SwiftData
  用户 ──▶ 跟进管理Service ──▶ SwiftData
  跟进管理Service ──▶ 通知调度Service ──▶ iOS通知中心
  跟进管理Service ──▶ AI内容生成Service ──▶ Apple Foundation Models
  用户 ──▶ 模板管理Service ──▶ SwiftData
  iOS联系人 ──▶ 客户管理Service ──▶ SwiftData
  RevenueCat ──▶ 订阅管理Service ──▶ 功能解锁
```

---

## 七、极具竞争力的价格策略

### 7.1 价格策略深度分析

**市场定价参考**:

| 竞品 | 免费额度 | 付费价格 | 模式 |
|------|----------|----------|------|
| FollowUp (Never Lose Touch) | 基础功能免费 | IAP | 免费增值 |
| LeedsApp | 7天试用 | $0.99/周, $12.99/年 | 订阅 |
| Pingnote | 100联系人+5跟进 | $9.99/月 | 免费增值 |
| Follow-Up AI | 3次AI跟进 | $9.99/月或$99.99一次性 | 混合 |
| OnePageCRM | 7天试用 | $15/用户/月 | 纯订阅 |
| Clientloop | 有限试用 | 订阅制 | 订阅 |
| Reminderbase | 有限 | $2.99-13.99/月 | 分级订阅 |

### 7.2 推荐价格策略：站内订阅A模式

**选择理由**：FollowUpNow属于功能简单软件，核心是通知提醒+AI内容生成。AI生成有API成本（Apple Foundation Models本地免费，但降级方案可能需要云端API），因此采用"免费下载+免费功能+一次性订阅买断"模式。

**详细定价方案**:

```
┌────────────────────────────────────────────────────────────────┐
│                    FollowUpNow 定价方案                         │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  🆓 免费版（永久免费，引流用）                                   │
│  ├── 最多10个客户                                              │
│  ├── 最多5个活跃跟进提醒                                       │
│  ├── 基础通知提醒                                              │
│  ├── 手动编写跟进内容                                          │
│  ├── 1个跟进模板                                               │
│  └── 简单管道视图                                              │
│                                                                │
│  ⭐ Pro版（一次性买断 $14.99）                                  │
│  ├── 无限客户                                                  │
│  ├── 无限跟进提醒                                              │
│  ├── AI跟进内容生成（本地Apple Foundation Models，无API成本）    │
│  ├── 无限跟进模板                                              │
│  ├── 交互历史时间线                                            │
│  ├── Widget桌面小组件                                          │
│  ├── 通知操作快捷按钮（Call/Text/Snooze/Done）                 │
│  ├── 联系人自动导入                                            │
│  ├── 数据导出（CSV/PDF）                                       │
│  └── 通知隐私模式                                              │
│                                                                │
│  💎 Pro+版（$3.99/月 或 $29.99/年）                            │
│  ├── 包含Pro版所有功能                                         │
│  ├── 云端AI生成（GPT-4o-mini降级方案，有API成本）              │
│  ├── iCloud跨设备同步                                          │
│  ├── 智能跟进建议（基于交互频率分析）                           │
│  ├── 批量跟进操作                                              │
│  ├── 自定义通知声音                                            │
│  └── 优先客服支持                                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 7.3 定价策略详细分析

**为什么选择这个定价方案？**

| 分析维度 | 详细说明 |
|----------|----------|
| **免费版引流** | 10客户+5跟进足够让用户体验核心价值，但不足以日常使用，自然驱动升级 |
| **一次性买断$14.99** | 小企业主反感订阅，$14.99是心理甜点（<$15感觉不贵），且本地AI无持续成本，可一次性买断 |
| **Pro+订阅$3.99/月** | 云端AI有API成本，必须用订阅覆盖；$3.99/月远低于竞品$9.99/月，极具价格竞争力 |
| **年付$29.99** | 相当于$2.50/月，节省37%，鼓励年付锁定用户 |
| **无免费试用** | 免费版本身就是试用，降低用户获取摩擦 |

**收入预测模型**:

| 场景 | 月下载量 | 免费转Pro率 | 免费转Pro+率 | 月收入 |
|------|----------|-------------|-------------|--------|
| 保守 | 500 | 5% | 2% | $524 |
| 中等 | 2000 | 8% | 4% | $2,698 |
| 乐观 | 5000 | 10% | 6% | $8,994 |

**收入计算**:
- Pro一次性: 下载量 × 转化率 × $14.99
- Pro+月付: 下载量 × 转化率 × $3.99 × 累计月数
- Pro+年付: 假设30%选年付，下载量 × 转化率 × 0.3 × $29.99/12

### 7.4 付费墙设计策略

```
付费墙触发时机：
━━━━━━━━━━━━━━
1. 客户数达到10个 → "升级Pro解锁无限客户"
2. 活跃跟进达到5个 → "升级Pro解锁无限跟进"
3. 点击AI生成按钮 → "升级Pro使用AI跟进助手"
4. 使用第2个模板 → "升级Pro解锁无限模板"
5. 点击Widget设置 → "升级Pro添加桌面小组件"
6. 点击云同步 → "升级Pro+开启跨设备同步"

付费墙UI设计原则：
━━━━━━━━━━━━━━━━
- 展示具体功能对比表
- 突出"一次性买断$14.99"选项（默认选中）
- Pro+作为"推荐"标签
- 不使用暗黑模式（不阻止用户关闭弹窗）
- 提供"稍后提醒"选项
```

---

## 八、符合美国市场的UI设计

### 8.1 设计原则

| 原则 | 说明 | 实现方式 |
|------|------|----------|
| **极简主义** | 美国用户偏爱简洁界面，零学习成本 | 每屏最多3个操作按钮 |
| **即时反馈** | 操作后立即看到结果 | Haptic反馈+动画+状态更新 |
| **移动优先** | 80%使用场景在移动中 | 大按钮，单手操作友好 |
| **信息密度适中** | 不过载也不空洞 | 卡片式布局，关键信息一眼可见 |
| **Apple原生感** | 用户期望与系统一致的体验 | SwiftUI原生组件，SF Symbols |

### 8.2 色彩系统

```
主色调：
├── Primary Blue:    #007AFF (系统蓝，信任+专业)
├── Success Green:   #34C759 (完成/转化)
├── Warning Orange:  #FF9500 (待跟进/联系中)
├── Danger Red:      #FF3B30 (逾期/紧急)
└── Neutral Gray:    #8E8E93 (辅助文字)

背景色：
├── Light Mode:      #FFFFFF (纯白) + #F2F2F7 (分组背景)
└── Dark Mode:       #000000 (纯黑) + #1C1C1E (分组背景)

强调色：
└── Accent:          #5856D6 (紫色，AI功能标识)
```

### 8.3 页面结构

```
TabBar结构（4个Tab）：
━━━━━━━━━━━━━━━━━━━
┌────┬────┬────┬────┐
│ 🏠 │ 👥 │ 📊 │ ⚙️ │
│Home│People│Pipe│Set│
└────┴────┴────┴────┘

Tab 1: Home (Dashboard)
├── 今日跟进列表（核心视图）
├── 统计卡片（Due Today/Overdue/This Week/Clients）
├── 筛选器（Today/Overdue/This Week/All）
└── 快速添加按钮（+）

Tab 2: People (客户列表)
├── 搜索栏
├── 按管道阶段分组
├── 最近交互时间排序
└── 联系人首字母索引

Tab 3: Pipeline (管道视图)
├── 3列看板（New → Contacted → Converted）
├── 拖拽移动客户
└── 每列显示客户数和总金额

Tab 4: Settings (设置)
├── Pro/Pro+升级入口
├── 通知偏好
├── 默认提醒时间
├── 模板管理
├── 数据导出
├── 隐私设置
└── 关于/帮助
```

### 8.4 关键页面UI描述

**Dashboard (首页)**:
```
┌─────────────────────────────────┐
│  FollowUpNow          [+ 按钮] │
├─────────────────────────────────┤
│ ┌──────┐ ┌──────┐ ┌──────┐    │
│ │  3   │ │  1   │ │  7   │    │
│ │Due   │ │Over  │ │This  │    │
│ │Today │ │due   │ │Week  │    │
│ └──────┘ └──────┘ └──────┘    │
├─────────────────────────────────┤
│ [Today] [Overdue] [Week] [All] │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐│
│ │ 🔴 John Smith - Acme Corp  ││
│ │    Call back about proposal ││
│ │    Overdue by 2 days        ││
│ │    [📞 Call] [💬 Text]     ││
│ └─────────────────────────────┘│
│ ┌─────────────────────────────┐│
│ │ 🟠 Sarah Johnson            ││
│ │    Send follow-up email     ││
│ │    Due today at 3:00 PM     ││
│ │    [📧 Email] [✨ AI]      ││
│ └─────────────────────────────┘│
│ ┌─────────────────────────────┐│
│ │ 🔵 Mike Davis - TechStart  ││
│ │    Schedule demo call       ││
│ │    Due tomorrow at 10:00 AM ││
│ │    [📞 Call] [⏰ Snooze]   ││
│ └─────────────────────────────┘│
└─────────────────────────────────┘
```

**客户详情页**:
```
┌─────────────────────────────────┐
│  ← Back          [Edit] [⋯]   │
├─────────────────────────────────┤
│  👤 John Smith                  │
│  🏢 Acme Corporation           │
│  📧 john@acme.com              │
│  📱 (555) 123-4567             │
│  🏷️ Stage: Contacted           │
├─────────────────────────────────┤
│  [📞 Call] [💬 Text] [📧 Email]│
├─────────────────────────────────┤
│  Upcoming Follow-Ups            │
│  ┌─────────────────────────────┐│
│  │ 📋 Send proposal - May 6   ││
│  │ 📋 Check in - May 15       ││
│  └─────────────────────────────┘│
│  [+ Add Follow-Up]             │
├─────────────────────────────────┤
│  Interaction History            │
│  ┌─────────────────────────────┐│
│  │ 📞 May 1 - Discussed needs ││
│  │ 📧 Apr 28 - Sent intro     ││
│  │ 🤝 Apr 25 - Met at event   ││
│  └─────────────────────────────┘│
├─────────────────────────────────┤
│  Notes                          │
│  Budget: ~$5K, Decision by June │
└─────────────────────────────────┘
```

### 8.5 动画与交互设计

| 交互 | 动画效果 | Haptic |
|------|----------|--------|
| 完成跟进 | 卡片滑出+绿色勾号动画 | .success |
| 贪睡跟进 | 卡片轻微摇晃+时钟旋转 | .warning |
| 逾期提醒 | 卡片左侧红色脉冲条 | .error |
| 管道拖拽 | 卡片浮起+阴影+磁吸效果 | .selection |
| AI生成 | 紫色光晕+文字逐字出现 | .alignment |
| 新客户添加 | 从底部弹入+弹性效果 | .lightImpact |

### 8.6 Widget设计

**小部件（2x2）**:
```
┌──────────────┐
│ FollowUpNow  │
│              │
│     3        │
│  Due Today   │
│              │
│  1 Overdue ⚠│
└──────────────┘
```

**中等部件（2x4）**:
```
┌──────────────────────────────┐
│ FollowUpNow        3 Due    │
├──────────────────────────────┤
│ 🔴 John Smith - Call back   │
│ 🟠 Sarah J - Send email     │
│ 🔵 Mike D - Schedule demo   │
└──────────────────────────────┘
```

---

## 九、可二次开发项目参考实例

### 9.1 keep-in-touch改造清单

| 改造项 | 优先级 | 改造内容 | 参考代码位置 |
|--------|--------|----------|-------------|
| 数据模型扩展 | P0 | 增加PipelineStage、FollowUpTemplate模型 | StayInTouch/Models/ |
| 商务化改造 | P0 | 从"友谊维护"改为"客户跟进"文案和逻辑 | StayInTouch/Views/ |
| AI内容生成 | P0 | 新增AIFollowUpService | 新建Services/ |
| 通知操作扩展 | P0 | 增加Call/Text/Snooze/Done操作 | StayInTouch/Services/NotificationManager.swift |
| 管道视图 | P1 | 新增3列看板视图 | 新建Views/PipelineView.swift |
| 模板系统 | P1 | 新增模板CRUD和自动填充 | 新建Views/TemplatesView.swift |
| RevenueCat集成 | P1 | 订阅和付费墙 | 新建Services/SubscriptionService.swift |
| Widget | P2 | 桌面小组件 | 新建WidgetExtension/ |
| iMessage集成 | P2 | 一键发送跟进消息 | StayInTouch/Views/ContactDetailView.swift |
| 数据导出 | P2 | CSV/PDF导出 | 新建Services/ExportService.swift |

### 9.2 ReminderWave参考模块

| 参考模块 | 用途 | 关键代码 |
|----------|------|----------|
| TaskServiceProtocol | 服务层协议设计 | ReminderWave/Services/TaskServiceProtocol.swift |
| LocalNotificationService | 通知调度实现 | ReminderWave/Services/LocalNotificationService.swift |
| 优先级颜色编码 | 视觉优先级系统 | ReminderWave/Extensions/ |
| MVVM架构 | ViewModel设计模式 | ReminderWave/ViewModels/ |

---

## 十、开发时间线与里程碑

| 周次 | 里程碑 | 交付物 |
|------|--------|--------|
| Week 1 | 基础架构 | Xcode项目+数据模型+导航骨架+CRUD Service |
| Week 2 | 核心功能 | Dashboard+客户管理+跟进创建+通知系统 |
| Week 3 | 差异化功能 | AI生成+模板系统+管道视图+联系人导入 |
| Week 4 | 商业化 | RevenueCat+付费墙+Widget+App Store资料 |
| Week 5 | 测试发布 | TestFlight Beta+Bug修复+性能优化+提交审核 |

---

## 十一、ASO优化策略

### 11.1 App Store标题与关键词

**标题**: FollowUpNow - Smart Client Follow-Up  
**副标题**: Never Lose a Client Again  

**关键词（100字符限制）**:
```
follow up,client reminder,CRM,sales follow up,contact reminder,business follow up,lead tracker,networking,client management,follow up reminder
```

### 11.2 App Store描述

```
Never lose a client again. FollowUpNow is the simplest way to stay on top of every follow-up.

FOR SMALL BUSINESS OWNERS, FREELANCERS & SALES PROS:
• Stop losing deals because you forgot to follow up
• No complicated CRM — just smart reminders that work
• AI-powered follow-up messages in seconds

KEY FEATURES:
✅ Smart Follow-Up Reminders — Set it and forget it. Get notified at exactly the right time.
✅ AI Message Generator — Stuck on what to say? AI writes personalized follow-ups for you.
✅ 3-Stage Pipeline — New → Contacted → Converted. Simple as it should be.
✅ One-Tap Actions — Call, text, or email directly from the notification.
✅ Follow-Up Templates — Save your best messages and reuse them instantly.
✅ Interaction History — Remember every conversation, every detail.

WHY FOLLOWUPNOW?
• 80% of sales need 5+ follow-ups, but 44% of reps give up after 1
• Most CRMs are overkill for small teams — we're not
• Set up in 30 seconds, not 30 minutes
• Your data stays on your device — privacy first

FREE FOREVER:
• Up to 10 clients and 5 active follow-ups
• Basic reminders and manual follow-ups

UPGRADE TO PRO ($14.99 one-time):
• Unlimited clients and follow-ups
• AI-powered message generation
• Unlimited templates
• Desktop widgets
• Data export

Download now and never lose a client again.
```

### 11.3 截图策略

| 截图顺序 | 内容 | 标题文案 |
|----------|------|----------|
| 1 | Dashboard今日视图 | "Never Miss a Follow-Up Again" |
| 2 | 通知操作快捷按钮 | "Take Action Right From Notification" |
| 3 | AI内容生成界面 | "AI Writes Your Follow-Ups For You" |
| 4 | 管道看板视图 | "Track Deals in 3 Simple Stages" |
| 5 | 模板系统 | "Save & Reuse Your Best Messages" |
| 6 | Widget小组件 | "See Today's Follow-Ups at a Glance" |

---

## 十二、风险分析与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| Apple Foundation Models不可用 | 中 | 高 | 降级到预设模板+GPT-4o-mini API |
| App Store审核被拒 | 低 | 高 | 确保隐私合规，无隐藏功能 |
| 竞品快速跟进 | 中 | 中 | 持续迭代AI功能，建立用户壁垒 |
| 用户不愿付费 | 中 | 中 | 免费版足够引流，Pro定价低于竞品 |
| 通知权限被拒绝 | 高 | 中 | 引导用户在设置中开启，提供替代方案 |

---

**文档生成时间**: 2026-05-04  
**数据来源**: Reddit, Hacker News, Twitter, GitHub, App Store, Exa, Bright Data  
**分析方法**: 多语言痛点挖掘 + 竞品分析 + GitHub项目评估 + 商业价值评估  
**参考项目**: slavins-co/keep-in-touch, emrepbu/ReminderWave, rizal72/iContactU  
**目标市场**: 美国B2B小企业用户
